*int[]
    index var
    fori(sıra garanti çünkü index'e göre gidiyor)
    foreach(sıra garanti değil)

*ArrayList
    index var : (get(i))
    fori(sıra garanti,çünkü index'e göre gidiyor)
    foreach(sıra garanti değil)

*Set
    index YOK
    fori YOK
    foreach (sıra garanti değil)
    garanti istiyorsan : ITERATOR yöntemi kullanacaksın